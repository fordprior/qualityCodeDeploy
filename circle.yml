machine:
  services:
    - docker

dependencies:
  post:
    - docker build -t $AWS_ACCOUNT_ID.dkr.ecr.us-east-1.amazonaws.com/newman:$CIRCLE_SHA1 .

test:
  post:
    - docker run -p 3000:3000 --name newman $AWS_ACCOUNT_ID.dkr.ecr.us-east-1.amazonaws.com/newman:$CIRCLE_SHA1 --url="https://www.getpostman.com/collections/17e59e33e2446cf09193"
