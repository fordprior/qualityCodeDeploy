machine:
  services:
    - docker

dependencies:
  post:
    - docker build -t $AWS_ACCOUNT_ID.dkr.ecr.us-east-1.amazonaws.com/newman:$CIRCLE_SHA1 .

test:
  post:
    - docker run -d -p 8080:8080 --name newman $AWS_ACCOUNT_ID.dkr.ecr.us-east-1.amazonaws.com/newman:$CIRCLE_SHA1; sleep 10
